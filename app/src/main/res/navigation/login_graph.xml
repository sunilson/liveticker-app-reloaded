<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            android:id="@+id/login_graph"
            app:startDestination="@id/loginFragment"
            xmlns:tools="http://schemas.android.com/tools"
        >

    <fragment
            android:id="@+id/loginFragment"
            android:name="at.sunilson.liveticker.login.presentation.login.LoginFragment"
            android:label="LoginFragment"
            tools:layout="@layout/fragment_login"
            >
        <action
                android:id="@+id/action_loginFragment_to_registerFragment"
                app:destination="@id/registerFragment"
                app:enterAnim="@anim/move_in_from_right"
                app:exitAnim="@anim/move_out_to_left_slightly"
                app:popEnterAnim="@anim/move_in_from_left_slightly"
                app:popExitAnim="@anim/move_out_to_right"
                />
    </fragment>
    <fragment
            android:id="@+id/registerFragment"
            android:name="at.sunilson.liveticker.login.presentation.register.RegisterFragment"
            android:label="RegisterFragment"
            tools:layout="@layout/fragment_register"
            />
</navigation>